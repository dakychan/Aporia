# A bot in aporia cheat client for PVP, PVE, and chat. Fulli with catboost and да бля агоритмы
## Что же я хочу видеть в этом монстре?
### Ну первая эта структура идет конечно же
мы ставим что
src/ai/aporia пакет
т.е
ai.aporia
что в нем будет?
ai.aporia.bot -> Сам бот, его логики, атаки, вектора, углы, ренндер

aporiac.cc.network  -> пакет для управления интереном, создание апи и ендпоинтов, создание и подняие с выключением локальхоста, прогрузка хтмл ксс в бразуер, сохранение в Filesmanager.ROOT /network/, поднятие поротов и осуществление передачи запросов GET POST и др

ai.aporia.network -> Сама логика сервера для бота, с которого уже будут собритьася catboot датасеты, обучение, поднятие порта и запросы, приняетие и конвертация запрсов 

ai.aporia.chat -> анализ чата ботом, сообщения игроков, сообщения тебя по нику сессии майна, анализ системы через OsManager

ну создав tree
можно ебануть
src/aporia.cc/network ->
 | EthernetUtils -> логика интерентеа, поднятие и закрытие порта, слушаниепорта и заведывание всеми последюующими их поднятием по init, расставление эндпоинтов
 | Requiests -> логика запроов, их сруктура, байты, информация с нашим Logger в случае неудачи, get post и др виды. Сделай тима requiests.sendReq(дата)
 | HTMLLogic -> логика хтмл, их сохранение по filesmanager.root./network/, ксс стили, шрифты все суда

 src/ai/aporia ->
  chat ->
    | ChatReader -> сам читатель чата на Catboost, распарс в .., сохранение сообщений для датасета в .apr формат конечно же
    | ChatParser -> оправка в чат сообщеинй от бота лица, тю.е мы крутим в Commands наши команду bot chat ... и там уже send, reply т.е указвыет $NICK, %messange%, sorry типо извиняется перед типом, troll тролирует
  network -> Ну че там, хтмл создание, поднятие порта допустим 61033, там ставим 
    | AiRequests -> запросы catboost, бота на сервер имено этот по порту ну и се впринцепи че там ну
    | NetowrkAimanager -> менежер по невторку, поднимает сервак, парсит хтмл и кеширует создавя их если нету
  bot -> ухх самео вкусное
    | AporiaBot -> Сам бот, мозг, подключение как физический игрок через твой интернет, интеграция catboost, схватывание всех ниш ниже
    | CatboostManage -> Кетбуст менежер, сохранение, загрузка, удаление датасета, понимае и сравнивание последних датасетов, удаление старых уже не нужных, создание новых через FilesManager
    | ChatBotManager -> схватывание всего их chat/ пакета
    | AporiaTrainingBot -> обучение кторе желательно бы разбить на пакеt ai.aporia.bot.training
    |   там будут, как парсер движения, последнего игрока вблизи, состояния здоровия, брони , еды, скила по движениям, эмоциям через чаз

вроде бы эта хуйня все
можно далее
### РЕализация
ухх ты
ну тут да пизда
крч нам надо с тобою
как то 
1. реализовать парсер чата в полный .txt или json как удобнее читать в будущем будет по пути сохранения filesmanager.root./bot/chat/ и там по датам т.е дата чата
в одну дату папку может помещаться несколько файликов о чате сегоднешнего дня
2. забыл сказать мы инет нахуй шлем и создаем по filesmanager уже структуру какую
а хуй такую
~/.apr/bot/network -> нетворк штуки там /html/ страницы, запросы мы не храним
~/.apr/bot/chat -> сам чат
~/.apr/bot/training -> обучение бота, т.е датасеты в gson также в папках по датам, также сохранение для каждой папки .cbm мдели
а нет даже лучше
для каждого дня сохранение .cbm
и в последующем сравнение
т.е смотри
есть например
2026-04-12 
это папка дня да из OsManager дату берем
внутри нее ещее папки уже со временем без секунд
когда заходили и это тоже учитываем для модели
в конец дня т.е при последнем закрытии майна
вечером или ночью учитывая
мы сохраняем .cbm

3. воот
терь к боту
как эту хуйню реализовать
у меня в планах лично
сажать бота как отдельного игрока
который может двигаться по агларитмам, бить, смотреть, крутиться,
без аи
это будет чуточку сложно
но пусть будет
